pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Progressive Overload"
include ':app', ':JavaRandoms'
project(':JavaRandoms').projectDir = new File('app/external/JavaRandoms/app')
